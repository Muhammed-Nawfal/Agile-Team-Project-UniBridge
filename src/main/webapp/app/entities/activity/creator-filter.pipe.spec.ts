import { CreatorFilterPipe } from './creator-filter.pipe';

describe('CreatorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
